╔══════════════════════════════════════════════════════════════════════════════╗
║                    CAPSTONE — COMPLETE RUN GUIDE                             ║
║              FRONTEND + BACKEND + GROQ MODEL SETUP                           ║
╚══════════════════════════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0. UPDATED requirements.txt  
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PASTE THE COMPLETE REQUIREMENTS.TXT FROM HERE ⬇️

# ============================================================
# CAPSTONE - Python Backend Requirements
# Python: 3.11
# ============================================================

# ------------------------------------------------------------
# CORE / FASTAPI WEB SERVER
# ------------------------------------------------------------
fastapi==0.124.0
uvicorn==0.33.0
pydantic>=2.0.0
python-multipart>=0.0.18
python-dotenv==1.0.1

# ------------------------------------------------------------
# LLM / AI
# ------------------------------------------------------------
groq==0.33.0

# ------------------------------------------------------------
# EMBEDDINGS & VECTOR DATABASE
# ------------------------------------------------------------
sentence-transformers==3.2.1
torch==2.4.1
numpy==1.24.4
chromadb==0.5.23

# ------------------------------------------------------------
# DOCUMENT PROCESSING & OCR
# ------------------------------------------------------------
pdfplumber==0.11.5
python-docx==1.1.2
PyMuPDF==1.24.11
pillow==10.4.0
pytesseract==0.3.13
ocrmypdf==14.4.0
pikepdf>=8.15.1,<10
opencv-python-headless<4.10.0

# ============================================================
# AFTER INSTALLATION - CHECK
# ============================================================
# python -m pip check
#
# For the CAPSTONE backend:
#   cd V:\Projects\CAPSTONE\backend
#   .\venv\Scripts\Activate.ps1
#   python -m uvicorn chat_api:app --reload
#
# Frontend:
#   cd V:\Projects\CAPSTONE\frontend
#   npm install
#   npm run dev
#
# Keep the Groq API key in backend\.env.
# Never put the API key directly into source code.

upto here ⬆️

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. PROJECT STRUCTURE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PROJECT ROOT
    V:\Projects\CAPSTONE\

    ├── frontend\
    │   ├── package.json
    │   ├── src\
    │   └── ...
    │
    ├── backend\
    │   ├── chat_api.py
    │   ├── .env
    │   ├── documents\
    │   ├── knowledge_base\
    │   └── ...
    │
    ├── requirements.txt
    └── Run.txt


IMPORTANT
    The project has TWO separate parts:

    [ FRONTEND ]
        React / Vite

    [ BACKEND ]
        FastAPI / Uvicorn

    You normally need TWO terminals open.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. FIRST-TIME SETUP / AFTER RESTORING THE LAPTOP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


┌──────────────────────────────────────────────────────────────────────────────┐
│ TERMINAL 1 — FRONTEND SETUP                                                 │
└──────────────────────────────────────────────────────────────────────────────┘

STEP 1 — Go to the frontend folder

    cd V:\Projects\CAPSTONE\frontend


STEP 2 — Check Node.js and npm

    node -v
    npm -v


STEP 3 — Install frontend packages

    npm install

This creates/restores:

    V:\Projects\CAPSTONE\frontend\node_modules\

You normally do NOT need to run npm install every time.

Run it again when:
    • node_modules is missing
    • package.json changed
    • package-lock.json changed
    • frontend dependencies need to be restored


STEP 4 — Start the frontend

    npm run dev

Vite will display a Local URL, for example:

    http://localhost:8080/

The port may be different.
Always open the Local URL shown by Vite.

KEEP TERMINAL 1 OPEN.


┌──────────────────────────────────────────────────────────────────────────────┐
│ TERMINAL 2 — BACKEND SETUP                                                  │
└──────────────────────────────────────────────────────────────────────────────┘

STEP 1 — Go to the backend folder

    cd V:\Projects\CAPSTONE\backend


STEP 2 — Create the Python virtual environment

Only do this if the venv folder does not exist:

    python -m venv venv


STEP 3 — Activate the virtual environment

PowerShell:

    .\venv\Scripts\Activate.ps1

You should see:

    (venv) PS V:\Projects\CAPSTONE\backend>


STEP 4 — Upgrade pip

    python -m pip install --upgrade pip


STEP 5 — Install backend requirements

    python -m pip install -r ..\requirements.txt


STEP 6 — Check installed dependencies

    python -m pip check

If this reports dependency conflicts, fix the requirements first.
Do not randomly install packages.


WINDOWS NOTE
If Windows blocks pip.exe, use:

    python -m pip ...

instead of:

    pip ...


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. NORMAL STARTUP — EVERY TIME
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

You do NOT need to recreate the venv every time.


┌──────────────────────────────────────────────────────────────────────────────┐
│ TERMINAL 1 — START FRONTEND                                                 │
└──────────────────────────────────────────────────────────────────────────────┘

    cd V:\Projects\CAPSTONE\frontend

    npm run dev

Keep this terminal open.

Open the Local URL printed by Vite.


┌──────────────────────────────────────────────────────────────────────────────┐
│ TERMINAL 2 — START BACKEND                                                   │
└──────────────────────────────────────────────────────────────────────────────┘

    cd V:\Projects\CAPSTONE\backend

    .\venv\Scripts\Activate.ps1

    python -m uvicorn chat_api:app --reload

Keep this terminal open.

Backend normally runs at:

    http://127.0.0.1:8000


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. CHECK WHETHER THE BACKEND IS RUNNING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Open this in your browser:

    http://127.0.0.1:8000/health

Expected response:

    {"status":"ok"}

If this works:
    FastAPI + Uvicorn are running correctly.

If you get:

    ModuleNotFoundError

copy the complete error before installing anything else.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. GROQ MODEL — WHAT CHANGED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

OLD MODEL
─────────

    llama-3.1-8b-instant


WHY THE OLD MODEL CAUSED THE ERROR
───────────────────────────────────

Your application previously used:

    llama-3.1-8b-instant

Groq deprecated/retired this model for the relevant developer/free usage,
which is why your application can return:

    404
    model_not_found

IMPORTANT
    The old model name may still appear inside:

    venv\Lib\site-packages\groq\

    DO NOT edit those files.

Those are installed Groq SDK files.


NEW MODEL
─────────

Use:

    openai/gpt-oss-20b

This is the replacement to use in your application for the old model.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. CREATE A NEW GROQ API KEY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Open the official Groq API Keys page:

    https://console.groq.com/keys


STEPS
─────

    1. Log in to Groq.
    2. Open API Keys.
    3. Click "Create API Key".
    4. Display Name:
           CAPSTONE
       (or any name you recognize)
    5. Choose the expiration you want.
    6. Create the key.
    7. Copy the key and store it safely.


API KEY FORMAT
──────────────

It normally looks similar to:

    gsk_XXXXXXXXXXXXXXXXXXXXXXXX

The example above is NOT a real key.


SECURITY
────────

NEVER put your real API key in:

    • Run.txt
    • GitHub
    • screenshots
    • chat messages
    • frontend source code
    • public repositories

Keep the key inside the backend .env file.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. PUT THE NEW GROQ API KEY IN .ENV
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Open:

    V:\Projects\CAPSTONE\backend\.env


If your backend expects GROQ_API_KEY, use:

    GROQ_API_KEY=YOUR_NEW_GROQ_API_KEY

Replace the placeholder with your actual key.

DO NOT copy the placeholder literally.

If your existing chat_api.py uses a different environment-variable name,
keep that exact existing name.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. REPLACE THE GROQ MODEL NAME
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

OLD:

    model="llama-3.1-8b-instant"


NEW:

    model="openai/gpt-oss-20b"


WHERE TO CHANGE IT
───────────────────

Change the model in YOUR application code/configuration.

Do NOT change:

    venv\Lib\site-packages\groq\...


Those are SDK files.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. FIND WHERE YOUR APPLICATION USES THE OLD MODEL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

From:

    V:\Projects\CAPSTONE\backend


Run this PowerShell command:

    Get-ChildItem -Path . -Recurse -File -Include *.py,*.env,*.json,*.yaml,*.yml,*.txt | Where-Object { $_.FullName -notmatch "\\venv\\" } | Select-String -Pattern "llama-3.1-8b-instant"


WHY THIS COMMAND?
─────────────────

It searches your project while excluding:

    venv\

So old model references inside the installed Groq SDK will not confuse you.


IF IT FINDS SOMETHING LIKE:

    chat_api.py: model="llama-3.1-8b-instant"


CHANGE IT TO:

    chat_api.py: model="openai/gpt-oss-20b"


You can also search for:

    GROQ_API_KEY

and:

    model=


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10. IF THE MODEL NAME IS STORED IN .ENV
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

If you have:

    GROQ_MODEL=llama-3.1-8b-instant

change it to:

    GROQ_MODEL=openai/gpt-oss-20b


If chat_api.py directly contains:

    model="llama-3.1-8b-instant"

change that instead.

Do NOT blindly change both.
Use the configuration method that your chat_api.py actually uses.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
11. RESTART BACKEND AFTER CHANGING MODEL OR API KEY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

If Uvicorn is running:

    Press Ctrl + C


Then:

    cd V:\Projects\CAPSTONE\backend

    .\venv\Scripts\Activate.ps1

    python -m uvicorn chat_api:app --reload


Check:

    http://127.0.0.1:8000/health

Then open the frontend and test the chatbot.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
12. IF YOU STILL GET model_not_found
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Check these one by one:

    [1] Model name is exactly:

        openai/gpt-oss-20b


    [2] New API key is inside:

        backend\.env


    [3] The backend was restarted after changing .env.


    [4] You are using the intended Groq account/project.


    [5] The API key is valid and has not expired/revoked.


    [6] Your Groq account has available usage/access.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
13. OLD MODEL NAME INSIDE venv — DO NOT WORRY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

If this search:

    Get-ChildItem -Path . -Recurse -File -Include *.py,*.env,*.json,*.yaml,*.yml,*.txt |
    Select-String -Pattern "llama-3.1-8b-instant"

shows matches under:

    venv\Lib\site-packages\groq\

DO NOT edit them.

The installed Groq SDK can contain old model IDs in its generated files.

Your application only needs to use the new model.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
14. FRONTEND BROWSERSLIST WARNING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

If Vite shows:

    Browserslist: browsers data (caniuse-lite) is old

This is a WARNING, not a frontend failure.

Your application can still run.

Optional update:

    cd V:\Projects\CAPSTONE\frontend

    npx update-browserslist-db@latest

    npm install

Then restart:

    npm run dev


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
15. IF node_modules IS MISSING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    cd V:\Projects\CAPSTONE\frontend

    npm install

    npm run dev


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
16. IF venv IS MISSING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    cd V:\Projects\CAPSTONE\backend

    python -m venv venv

    .\venv\Scripts\Activate.ps1

    python -m pip install --upgrade pip

    python -m pip install -r ..\requirements.txt

    python -m pip check

    python -m uvicorn chat_api:app --reload


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
17. FULL REBUILD — ONLY IF NECESSARY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

BACKEND
───────

    cd V:\Projects\CAPSTONE\backend

    deactivate

Remove the old venv folder only if you intentionally want a clean
Python environment.

Then:

    python -m venv venv

    .\venv\Scripts\Activate.ps1

    python -m pip install --upgrade pip

    python -m pip install -r ..\requirements.txt

    python -m pip check

    python -m uvicorn chat_api:app --reload


FRONTEND
────────

    cd V:\Projects\CAPSTONE\frontend

    npm install

    npm run dev


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
18. DO NOT DO THESE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    ✗ Do NOT run pip install from the frontend folder.

    ✗ Do NOT run npm install from the backend folder.

    ✗ Do NOT randomly install packages when requirements fail.

    ✗ Do NOT edit files inside venv\Lib\site-packages\groq\
      to change your application's model.

    ✗ Do NOT put the Groq API key in frontend source code.

    ✗ Do NOT commit backend\.env to GitHub.

    ✗ Do NOT run two Uvicorn servers on the same port.

    ✗ Do NOT close Terminal 1 while testing the frontend.

    ✗ Do NOT close Terminal 2 while testing the backend.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
19. NORMAL DAILY STARTUP — COPY/PASTE VERSION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

TERMINAL 1
──────────

    cd V:\Projects\CAPSTONE\frontend

    npm run dev


TERMINAL 2
──────────

    cd V:\Projects\CAPSTONE\backend

    .\venv\Scripts\Activate.ps1

    python -m uvicorn chat_api:app --reload


THEN:

    1. Open the Local URL shown by Vite.
    2. Open/test your application.
    3. Test the chatbot.
    4. If needed, check the backend:

       http://127.0.0.1:8000/health


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
20. FINAL CHECKLIST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    [ ] Node.js installed
    [ ] npm works
    [ ] frontend\node_modules exists
    [ ] Frontend starts with npm run dev

    [ ] backend\venv exists
    [ ] venv is activated
    [ ] Backend requirements installed
    [ ] python -m pip check is clean

    [ ] backend\.env exists
    [ ] GROQ_API_KEY is present
    [ ] Real API key is kept private

    [ ] Old model is no longer used by application code
    [ ] New model = openai/gpt-oss-20b

    [ ] Uvicorn starts:
        python -m uvicorn chat_api:app --reload

    [ ] /health returns:
        {"status":"ok"}

    [ ] Frontend opens
    [ ] Chatbot responds successfully


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
21. QUICK REFERENCE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PROJECT ROOT
    V:\Projects\CAPSTONE

FRONTEND
    V:\Projects\CAPSTONE\frontend

BACKEND
    V:\Projects\CAPSTONE\backend

FRONTEND START
    cd V:\Projects\CAPSTONE\frontend
    npm run dev

BACKEND START
    cd V:\Projects\CAPSTONE\backend
    .\venv\Scripts\Activate.ps1
    python -m uvicorn chat_api:app --reload

BACKEND HEALTH
    http://127.0.0.1:8000/health

OLD GROQ MODEL
    llama-3.1-8b-instant

NEW GROQ MODEL
    openai/gpt-oss-20b

GROQ API KEY PAGE
    https://console.groq.com/keys

ENV FILE
    V:\Projects\CAPSTONE\backend\.env

ENV VARIABLE
    GROQ_API_KEY=YOUR_NEW_GROQ_API_KEY


╔══════════════════════════════════════════════════════════════════════════════╗
║                           REMEMBER                                          ║
║                                                                              ║
║  Keep the real API key PRIVATE.                                             ║
║  Change the model in application code/configuration, NOT inside venv.        ║
║  Restart Uvicorn after changing the model or API key.                        ║
║  Use two terminals: one for frontend and one for backend.                   ║
╚══════════════════════════════════════════════════════════════════════════════╝
